services:
  valentines-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: valentines_site
    volumes:
      - ./:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true
