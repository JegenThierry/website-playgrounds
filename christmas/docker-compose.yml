services:
  christmas-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: christmas-site
    volumes:
      - ./:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - nginx-network

networks:
  nginx-network:
    external: true
